{
  "name": "token-usage-tracker-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute token-usage-tracker --file=./migrations/0001_init.sql",
    "db:init:local": "wrangler d1 execute token-usage-tracker --local --file=./migrations/0001_init.sql",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250730.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.99.0"
  }
}
